# 隐私说明

大何的待办事项是本地优先的待办工具。

## 数据存储

- 桌面端默认保存到 `%USERPROFILE%\Documents\DaheTodo\tasks.json`。
- 用户可以在桌面端“设置”里修改保存位置。
- 浏览器版数据文件是 `data/tasks.json`，该文件已被 `.gitignore` 忽略。

## 网络

应用核心功能不需要联网，不会主动上传待办、评论、周报内容。

## 开源仓库

请不要把自己的 `tasks.json`、备份文件、安装包、`node_modules`、缓存目录提交到 GitHub。
